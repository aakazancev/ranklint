---
'@ranklint/cli': minor
---

`ranklint audit --html-output <file>` writes a self-contained html report in both modes and `--json-output` now works in monitor mode; CI presets gained Pages publishing for scheduled monitors (GitLab `.ranklint-pages`, GitHub `monitor.yml` with `pages: true`)
