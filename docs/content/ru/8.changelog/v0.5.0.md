---
title: "ranklint 0.5.0"
description: "`sitemap:reachable` gets a `maxProbes` option (default 100, 0 = unlimited) and reports how many sitemap URLs were left unverified instead of skipping them sile…"
date: 2026-09-19
navigation: false
---

<!-- generated:start -->

Дата: 2026-09-19

### Новое

- **@ranklint/checks**: `sitemap:reachable` gets a `maxProbes` option (default 100, 0 = unlimited) and reports how many sitemap URLs were left unverified instead of skipping them silently ([9d8b74c](https://github.com/aakazancev/ranklint/commit/9d8b74c))
- **@ranklint/cli**: `ranklint lighthouse --profile <name>` applies a config profile (e.g. a release profile with Lighthouse enabled); the json output now includes the effective lighthouse config. CI presets gained a profile-driven Lighthouse job ([1635e68](https://github.com/aakazancev/ranklint/commit/1635e68))
- **@ranklint/cli**: `ranklint audit --html-output <file>` writes a self-contained html report in both modes and `--json-output` now works in monitor mode; CI presets gained Pages publishing for scheduled monitors (GitLab `.ranklint-pages`, GitHub `monitor.yml` with `pages: true`) ([1d0dc84](https://github.com/aakazancev/ranklint/commit/1d0dc84))
- **@ranklint/reporters**: markdown and html reports group issues by rule (errors first, biggest groups first) instead of one flat table — OG/meta findings for the same rule are now in one place ([45fc2e0](https://github.com/aakazancev/ranklint/commit/45fc2e0))

### Исправления

- **@ranklint/checks**: Rule `docs` links now point at the documentation site (ranklint.dev) with a page per rule ([c6d7a17](https://github.com/aakazancev/ranklint/commit/c6d7a17))
- **@ranklint/checks**: `meta:og-required` reports all missing og tags in a single issue per page instead of one issue per tag ([454b496](https://github.com/aakazancev/ranklint/commit/454b496))
- **@ranklint/cli**: Docs links of the lighthouse-threshold and links:reachable issues point at the documentation site ([c53ad60](https://github.com/aakazancev/ranklint/commit/c53ad60))
- **@ranklint/cli, @ranklint/cli, @ranklint/core**: Crawl progress: `onPage` callback in core, CLI logs each crawled page to stderr (`[ranklint] 5/30 200 1234ms <url>`). Reachability HEAD requests are now wrapped in the crawl timeout — a hanging HEAD stalled the whole crawl forever ([ce46ae5](https://github.com/aakazancev/ranklint/commit/ce46ae5))

<!-- generated:end -->
